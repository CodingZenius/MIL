Drop a looping runner/treadmill-POV video here named `running_loop.mp4`
(silent, 10-20s loop recommended, H.264 MP4, under ~15MB for reasonable APK size).

Until you add it, RunningScreen automatically falls back to a gradient
background — the app builds and runs fine either way.
